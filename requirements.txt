# OpenFable - Verified Output Engine
# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.0.0

# Verify loop
git+https://github.com/KingLabsA/reason-critic.git

# GPU controller (stdlib only - no external deps)
